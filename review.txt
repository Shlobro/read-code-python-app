1. High - `add-new-problems-auto-workflow.json:198`, `add-new-problems-auto-workflow.json:271-273`, `add-new-problems-auto-workflow.json:297-298`
The new "Validate difficulty placement" prompt tells the model to review `git diff --cached` first, but at that point in the graph nothing has been staged yet. The repo-wide `git add` does not happen until `6bd03f7f-6595-41a1-ae17-808143fc1f5f` finishes and routes to the stage node at line 271. In practice this means the validator is being pointed at an empty staged diff during the only point where it is supposed to catch bad difficulty placement before review starts.
How I would fix it: either move a `git add` node to run before `Validate difficulty placement`, or change that prompt to inspect `git diff` plus any untracked files directly at that stage instead of prioritizing `git diff --cached`.

2. Medium - `add-new-problems-auto-workflow.json:150`, `add-new-problems-auto-workflow.json:180`, `add-new-problems-auto-workflow.json:189`, `readcode_developer_guide.md:37`, `app/app_developer_guide.md:14`, `app/src/main/java/com/example/readcode/readcode_developer_guide.md:13`
The guides now correctly describe the required layout: new batch files belong under `com.example.readcode.problems`, with `ProblemSeedData.kt` acting only as the aggregator. The generation prompts still do not say that. They only ask the model to add problems by difficulty. That means the workflow still relies on the review/fix loop to repair package placement and guide drift after the fact, so the process is not actually aligned with the new docs.
How I would fix it: update each generation prompt to require writing batch additions under `app/src/main/java/com/example/readcode/problems/`, updating `ProblemSeedData.kt` imports/concatenation, and keeping the relevant developer guides in sync when structure changes.

3. Low - `app/src/main/java/com/example/readcode/problems/ProblemSeedDataHard5.kt:48`
`output_hard_5` looks under-placed for the newly documented hard bar. The surrounding guides define hard as senior-level language/runtime behavior rather than straightforward syntax or tracing. This question is a fairly standard decorator-order trace and is materially easier than the descriptor, metaclass, and generator-state questions around it.
How I would fix it: either move this problem to medium, or rewrite it so the decorator behavior depends on something more senior-level, such as descriptor binding on decorated methods, closure state shared across decorators, or `functools.wraps` / metadata propagation side effects.
