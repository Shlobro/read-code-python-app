# TODO

- Split `MainActivity.kt` into smaller files for models, seed problems, and screens before it approaches the file-size limit.
- Add more Python problems so each type and difficulty has a larger list.
- Persist completion state so progress survives app restarts.
- Add app-level language settings and later per-problem language overrides.
- Add folder-level developer guides when work starts inside deeper project directories.
